
  # Connect Scenes with Finish Button

  This is a code bundle for Connect Scenes with Finish Button. The original project is available at https://www.figma.com/design/C1oipCyLBLUt8dzLDPKm8s/Connect-Scenes-with-Finish-Button.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  